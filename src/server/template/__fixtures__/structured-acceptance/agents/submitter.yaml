id: submitter
name: Submitter Agent
description: Post-seal v2 agent that submits the final artifact.
model: configured/submitter-model
systemPrompt: |
  You are the submitter agent. Review the sealed artifact and submit the
  final output.
turnContract:
  version: 2
  dispatch:
    allowedActions: [submit_final_artifact]
    targets: {}

# 非法连线：route.to 指向不存在的 Agent（测试 fixture 数据）
agents:
  - writer
  - reviewer
routes:
  - from: writer
    to: ghost-agent
    kind: artifact
    label: 提交初稿
  - from: reviewer
    to: writer
    kind: message
    label: 退回意见
finalOutput:
  submitters:
    - reviewer
